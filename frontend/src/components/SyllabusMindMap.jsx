import React, { useState, useEffect } from 'react';
import { getStudyProgress, toggleTopicProgress } from '../utils/api';

const SyllabusMindMap = ({ subject }) => {
  // ... existing code ...
};

export default SyllabusMindMap;
